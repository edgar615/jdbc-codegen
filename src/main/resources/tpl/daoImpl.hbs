package {{package}};

import com.github.edgar615.util.db.BaseDaoImpl;
import com.github.edgar615.util.db.Jdbc;
import {{domainPackage}}.{{table.upperCamelName}};
import {{daoPackage}}.{{table.upperCamelName}}Dao;
{{#if supportSpring}}import org.springframework.stereotype.Service;{{/if}}

/**
* This class is generated by Jdbc code generator.
*
* Table : {{table.name}}
*
* @author Jdbc Code Generator Date {{date}}
*/
{{#if supportSpring}}@Service{{/if}}
public class {{table.upperCamelName}}{{suffix}} extends BaseDaoImpl<{{table.pkType.name}}, {{table.upperCamelName}}> implements {{table.upperCamelName}}Dao {

   public {{table.upperCamelName}}{{suffix}}(Jdbc jdbc) {
      super({{table.upperCamelName}}.class, jdbc);
   }

   {{safestr userSource}}
}