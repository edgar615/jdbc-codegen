<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.generated.code.repository.UserMapper">
	<resultMap id="UserResultMap" type="User">
		<result column="user_id" property="userId" />
		<result column="applicant_id" property="applicantId" />
		<result column="role_id" property="roleId" />
		<result column="username" property="username" />
		<result column="fullname" property="fullname" />
		<result column="password" property="password" />
		<result column="salt" property="salt" />
		<result column="organization" property="organization" />
		<result column="authority" property="authority" />
	</resultMap>

	<sql id="all_column">
		user_id, applicant_id, role_id, username, fullname, password, salt, organization, authority
	</sql>

	<sql id="limit">
		<if test="limit != null">
			limit
			<if test="offset != null">
			#{offset},
			</if>
			#{limit}
		</if>
	</sql>

	<insert id="insert" parameterType="User">
		insert into 
		user(java.util.ArrayList$Itr@4e41089d, ) 
		values(java.util.ArrayList$Itr@32a068d1, )
	</insert>

	<delete id="deleteByPrimaryKey" parameterType="int">
		delete from user 
		where user_id = #{id}
	</delete>

	<update id="updateByPrimaryKey" parameterType="User">
		update user
		<set>
 java.util.ArrayList$Itr@33cb5951		</set> 
		where user_id = #{userId}
	</update>

	<select id="selectByPrimaryKey" resultMap="UserResultMap" parameterType="int">
		select user_id, applicant_id, role_id, username, fullname, password, salt, organization, authority 
		from user 
		where user_id = #{id}
	</select>

	<!-- START 写在START和END中间的代码不会被替换 -->

	<!-- END 写在START和END中间的代码不会被替换-->

</mapper>